# This ROS message contains the name of the ROS node
# and the corresponding ID of its DDS participant

string node_name   # ROS node name
uint8[] id         # DDS participant's ID represented 
                   # as an array of integers
